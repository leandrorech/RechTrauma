# RechTrauma

Ferramenta de apoio ao atendimento inicial ao paciente traumático (protocolo/checklist estruturado, linha ATLS-like).

## Estado atual

Repositório em fase de **migração e reconciliação de artefatos físicos recuperados** — ainda sem versão canônica eleita. Ver `PROJECT_STATE.md` para o estado detalhado e atualizado.

Candidatas localizadas em `candidates/`:

- `rechtrauma_copiloto_v12.html` — versão v12, comprovada.
- `rechtrauma_v23.html` — candidata, XABCDE longitudinal, eFAST, Shock Index/SIA, ABC Score, RTS, PTM/DCR, neuro, airway.

Variantes adicionais (não reconciliadas) preservadas em `archive/recovered/`.

## Antes de editar

Leia `AGENTS.md` e `PROJECT_STATE.md` primeiro — definem o protocolo obrigatório de coordenação entre agentes: nenhuma candidata deve ser promovida a canônica sem diff/auditoria funcional e clínica, e qualquer mudança de estado/decisão deve ser refletida em `PROJECT_STATE.md` no mesmo commit.
